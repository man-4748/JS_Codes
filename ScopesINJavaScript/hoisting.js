gun();
function gun(){
    console.log("Hello World");
}

/**
 * Declaring a function before calling it
 * Hoisting is nothing but a direct consequence of Lexical scoping
 * Nothing such as moving functions to top of the code happens while parsing, its incorrect.
 */

